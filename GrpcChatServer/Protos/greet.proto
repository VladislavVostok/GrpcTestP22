syntax = "proto3";

option csharp_namespace = "GrpcChatServer";

package chat;

service Chat {
  rpc JoinChat (stream ChatMessage) returns (stream ChatMessage);
}

message ChatMessage {
  string user = 1;
  string text = 2;
}
